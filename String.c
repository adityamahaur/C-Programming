#include <stdio.h>
int main()
{
    char str[52];
    printf("Input The String: ");
    gets(str);
    printf("\n%s", str);
    // puts (str); alternate to just print screen

    return 0;
}