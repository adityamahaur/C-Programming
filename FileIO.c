#include<stdio.h>
int main()
{
    FILE *ptr = NULL;
    char string[64] = "This content is in Aditya's Laptop";


    /*
    Reading a file:
    ptr = fopen("text.txt", "r");
    fscanf(ptr, "%s", string);
    printf("The content of this file has %s\n", string);
    */


    /*
    Writing a file:
    ptr = fopen("text.txt", "w");
    fprintf(ptr, "%s", string);
    */
    // Write m poora change hojata h and append m bas add hojata h already jo file h usme
    
    return 0;
}